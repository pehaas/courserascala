package _3_2_classer_organization

object Traits {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(94); 
  println("Welcome to the Scala worksheet")}
  
  
  //Traits are like java interfaces but in contrast to java interfaces they can contain method implementations.
  //Traits can never contain parameters (class attributes)
  
  
  
}
